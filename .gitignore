# Private files
*.local
*.private
.env

# SSH keys (only backup config, not keys)
ssh/id_*
ssh/*.pub

# macOS
.DS_Store

# Raycast (backup manually)
raycast/*.rayconfig

# User-specific config (created by setup.sh)
config.local
